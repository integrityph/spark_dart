name: project_build_tool
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.8.0
  build_tool: 
    git:
      url: https://github.com/irondash/cargokit.git
      path: build_tool

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
